# F02 — Array: vários valores relacionados

> Referência conceitual: slides 9–20. Tempo típico: 2–8 minutos por checkpoint.

## Objetivo

Organizar estudantes e notas em coleções ordenadas e operar por posição.

## Arquivo de trabalho

`src/fases/f02-array.ts`

## Desafios

### F02-A01 — Criar Array (25 XP)

Faça `criarAlunos` devolver, nesta ordem, Ana, Bruno e Carla.

**Comportamento esperado:** o verificador deve confirmar a operação para diferentes dados quando a tarefa recebe parâmetros.

### F02-A02 — Acessar posição (25 XP)

Faça `obterAluno` devolver o item que está no índice recebido.

**Comportamento esperado:** o verificador deve confirmar a operação para diferentes dados quando a tarefa recebe parâmetros.

### F02-A03 — Lembrar o índice zero (25 XP)

Faça `primeiroAluno` devolver o primeiro item de qualquer Array não vazio.

**Comportamento esperado:** o verificador deve confirmar a operação para diferentes dados quando a tarefa recebe parâmetros.

### F02-A04 — Alterar posição (25 XP)

Em `alterarNota`, modifique a posição recebida no próprio Array e devolva esse Array.

**Comportamento esperado:** o verificador deve confirmar a operação para diferentes dados quando a tarefa recebe parâmetros.

### F02-A05 — Contar elementos (25 XP)

Faça `quantidadeDeAlunos` devolver quantos itens existem no Array.

**Comportamento esperado:** o verificador deve confirmar a operação para diferentes dados quando a tarefa recebe parâmetros.

### F02-A06 — Adicionar no fim (25 XP)

Em `adicionarAluno`, acrescente o nome ao fim do próprio Array e devolva esse Array.

**Comportamento esperado:** o verificador deve confirmar a operação para diferentes dados quando a tarefa recebe parâmetros.

## Ciclo de trabalho

Depois de cada TODO, salve e execute:

```bash
npm run check
```

Se precisar, peça somente o nível de dica necessário:

```bash
npm run dica -- F02-A01
```
