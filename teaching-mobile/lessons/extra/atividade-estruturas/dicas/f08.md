# Dicas — F08

Leia apenas a dica do checkpoint em que você está trabalhando.

## F08-A01

- DICA 1 — O tipo `AppDaTurma` já lista as quatro propriedades.
- DICA 2 — Use `[]`, `new Set` e `new Map` conforme cada propriedade.
- DICA 3 — Devolva um objeto com as quatro coleções vazias.

## F08-A02

- DICA 1 — Existem duas coleções que precisam crescer.
- DICA 2 — Use `push` em `alunos` e em `notasBimestrais`.
- DICA 3 — Adicione cada parâmetro à coleção correspondente e devolva o App.

## F08-A03

- DICA 1 — A propriedade `tecnologias` é um Set.
- DICA 2 — Use o método `add` nessa propriedade.
- DICA 3 — Modifique a coleção interna e devolva o App.

## F08-A04

- DICA 1 — A propriedade `notasPorAluno` é um Map.
- DICA 2 — Registre chave e valor com `set`.
- DICA 3 — Use nome como chave, média como valor e devolva o App.

## F08-A05

- DICA 1 — Comece encontrando o aluno pelo índice.
- DICA 2 — A nota usa a mesma linha e o bimestre como coluna; a média usa o nome como chave.
- DICA 3 — Monte um objeto `{ aluno, nota, media }` com valores consultados nas três estruturas.
